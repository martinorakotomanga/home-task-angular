import { DayTaskBgColorPipe } from './day-task-bg-color.pipe';

describe('DayTaskBgColorPipe', () => {
  it('create an instance', () => {
    const pipe = new DayTaskBgColorPipe();
    expect(pipe).toBeTruthy();
  });
});
