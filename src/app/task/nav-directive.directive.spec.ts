import { NavDirectiveDirective } from './nav-directive.directive';

describe('NavDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NavDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
